Creates a virtualenv bootstrap script:
Usage:
python create_bootstrap.py

The currently defined function just adds an after_install hook and pip installs from requirements.txt

Test out the script:
python bootstrap.py <virtualenvname>

cd <virtualenvname>
./bin/activate

